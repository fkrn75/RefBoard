# RefBoard 개발 체크리스트

> 이 문서는 RefBoard 개발 진행의 **단일 출처(SSOT)** 입니다. 작업이 끝날 때마다 `[ ]` → `[x]`로 갱신합니다.
> 상세 기능 근거는 [기능명세서.md](기능명세서.md) 참조.
> 최종 갱신: 2026-06-17

## 범례
- `[x]` 완료 · `[ ]` 예정 · `[~]` 진행/부분완료
- 🔴 **P0** MVP 필수 · 🟡 **P1** v1.0 · 🟢 **P2** 확장
- `(파일/함수)` = 구현 위치 힌트

## 진행 현황 요약
| Phase | 상태 | 비고 |
|---|---|---|
| 0. 셋업 & 인프라 | `[~]` 90% | git·README·공개 repo ✅, Rust·lint 남음 |
| 1. 캔버스 코어 (MVP) | `[~]` 98% | 핵심+소품 전부 ✅(fit-all·포커스·줌리셋·파일열기·로딩·디코드에러·GIF·커서·축고정·스냅·미니맵·z순서). 잔여=좌표HUD(🟢)뿐 |
| 2. 편집 도구 | `[~]` 80% | 기즈모·정렬/분배/정규화·뒤집기·z순서·**크롭(비파괴)**·변형리셋·**그룹·투명도·잠금·배경그리드** ✅. 잔여=노트/스포이드 |
| 3. 파일 & 내보내기 | `[ ]` 0% | — |
| 4. Tauri 데스크탑 셸 | `[ ]` 0% | Rust 설치 후 |
| 5. 웹 공유 (차별점) | `[ ]` 0% | core-canvas 재사용 |
| 6. 품질 & 마감 | `[ ]` 0% | — |

---

## Phase 0 — 프로젝트 셋업 & 인프라 `[~]`

### 0.1 개발 환경
- [x] Node / npm / bun / WebView2 확인 (Node 24.13, npm 11.6, bun 1.3)
- [x] 스택 확정: Tauri 2 + Vite + TypeScript + PixiJS(WebGL)
- [ ] 🟡 Rust 툴체인 설치 (`winget install Rustlang.Rustup` → `rustup default stable-msvc`)
- [ ] 🟡 MSVC C++ Build Tools 확인 (link.exe — rustup 안내 따름)
- [ ] 🟡 Tauri CLI 설치 (`npm i -D @tauri-apps/cli`) — Phase 4에서

### 0.2 프로젝트 구조
- [x] `app/` Vite + TS + PixiJS 스캐폴딩 (package.json, tsconfig, vite.config, index.html)
- [x] `npm install` 완료
- [x] `src/core/` (재사용 엔진) ↔ `src/` (앱 입력) 폴더 분리
- [x] dev 서버 등록 (`~/.claude/launch.json` 'refboard' 포트 1420)
- [x] 🔴 `.gitignore` (node_modules, dist, src-tauri/target)
- [x] 🟡 `git init` + 첫 커밋 (로컬 user.email=fkrn75@gmail.com) → 공개 repo **github.com/fkrn75/RefBoard**
- [x] 🟡 `README.md` (실행법, 구조 설명)
- [ ] 🟢 ESLint + Prettier (코드 스타일 통일)
- [ ] 🟢 npm audit 취약점 2건 점검

---

## Phase 1 — 캔버스 코어 (MVP) 🔴 `[~]`

> 목표: 설치 없이 브라우저에서 "이미지를 모으고·배치하고·저장"하는 최소 동작 보드.

### 1.1 캔버스 & 카메라 `[~]`
- [x] PixiJS Application 초기화 (`scene.ts` `Scene.create`)
- [x] world 컨테이너 1개를 이동/스케일 (무한 캔버스)
- [x] 휠 줌 (커서 위치 고정점) (`main.ts` wheel)
- [x] 우클릭/휠클릭 드래그 팬 (`main.ts` pointerdown/move/up)
- [x] 우클릭 컨텍스트 메뉴 차단
- [x] 창 리사이즈 자동 대응 (`resizeTo: host`)
- [x] 🔴 줌 한계 클램프(0.05~20) + `Ctrl+0` 줌 100% 리셋 (`main.ts` zoomReset)
- [x] 🔴 전체 보기 fit-all (`Ctrl+Space`) — `scene.contentBounds` AABB 후 카메라 맞춤
- [x] 🔴 이미지 포커스 fit (`Space` / 더블클릭) — 선택 이미지에 줌
- [x] 🟡 카메라 상태를 board.camera에 영구 동기화 (`applyCam`에서 갱신)
- [ ] 🟢 좌표/줌 레벨 인디케이터(HUD)
- [x] 🟢 미니맵 (`core/minimap.ts` Minimap, `M` 토글 + 클릭 점프)

### 1.2 이미지 가져오기 `[~]`
- [x] 드래그앤드롭 (이미지 파일 필터, 드롭 위치=월드 좌표) (`main.ts` drop)
- [x] 클립보드 붙여넣기 `Ctrl+V` (화면 중앙 배치) (`main.ts` paste)
- [x] 여러 파일 일괄 드롭 (offset으로 어긋나게)
- [x] data URL 임베드 + 원본 크기(natural) 측정
- [x] 🔴 파일 열기 다이얼로그 `Ctrl+I` (`openImageFiles`, `<input type=file multiple>`)
- [x] 🔴 로딩 인디케이터 (`importFiles` N/M 진행 표시 오버레이)
- [x] 🔴 디코드 실패/비이미지 에러 처리 + 토스트 알림 (`importFiles` 사전 검증→실패 건너뜀)
- [x] 🟡 GIF 애니메이션 표시 (`@pixi/gif` AnimatedGIF.fromBuffer, `scene.addImage` 분기)
- [ ] 🟢 URL 붙여넣기 → 페이지 이미지 스크랩 (CORS·프록시 필요, Phase 5 백엔드와 연계)
- [ ] 🟢 내장 스크린샷 캡처 (Tauri 단계)

### 1.3 선택 (Selection) `[x]`
- [x] 🔴 `selectedIds: Set<string>` 선택 상태 모듈 (`core/selection.ts` — Selection 클래스 + onChange 구독)
- [x] 🔴 스프라이트 hit-test (stage 전역 포인터 이벤트 + `e.target.label`로 id 식별)
- [x] 🔴 좌클릭 단일 선택 → selectedIds 교체
- [x] 🔴 선택 시각화: 외곽선 박스 (`scene.drawSelection`, 줌 보정 오버레이 레이어)
- [x] 🔴 Shift+좌클릭 토글 다중 선택
- [x] 🔴 빈 캔버스 좌클릭 → 선택 해제
- [x] 🔴 빈 곳 좌클릭 드래그 → 러버밴드(반투명 Graphics) → 교차 항목(AABB) 선택
- [x] 🔴 `Ctrl+A` 전체 선택 / `Esc` 해제
- [x] 🟡 경계상자 계산 (`scene.getItemAABB`/`corners`, 회전·스케일 고려)
- [x] 🟡 잠긴(locked) 항목 — 이동/기즈모 변형 차단 적용, 선택 외곽선 주황 구분(2.7 잠금기능)

### 1.4 이동 (Move) `[x]`
- [x] 🔴 선택 스프라이트 위 좌클릭 드래그 → 이동
- [x] 🔴 월드 좌표 delta로 `transform.x/y` 갱신 (줌 자동 보정)
- [x] 🔴 다중 선택 시 동일 delta 일괄 이동
- [x] 🔴 이동 중 `board.items[].transform` 동기화 (SSOT)
- [x] 🟡 커서 `grab` → `grabbing` 전환 (`scene.setCursor`, 드래그 동안)
- [x] 🟡 Shift 누르면 수평/수직 축 고정 이동
- [x] 🟢 이웃 가장자리 스냅 / 그리드 스냅 (`core/snap.ts`, `N` 토글·이웃 우선→그리드)

### 1.5 자동 패킹 (Arrange optimal) `[x]` — ⭐ 킬러 기능
- [x] 🔴 패킹 알고리즘 — Skyline bin packing (`core/pack.ts` `packImages`, packer 작성)
- [x] 🔴 목표 영역 종횡비 = 창 비율(host.clientWidth/Height)
- [x] 🔴 각 이미지 실제 크기(natural × scale) 수집
- [x] 🔴 패킹 결과 좌표를 `transform`에 적용(뷰 중앙 평행이동)
- [x] 🔴 항목 간 간격 padding (기본 16px)
- [x] 🔴 `Ctrl+P` 바인딩 (선택 2개+면 선택만, 아니면 전체)
- [x] 🟡 빈 보드 / 1장 예외 처리 (pack.ts 내장)
- [x] 🟡 패킹 후 fit-all 자동 호출 (`packAll` 끝 `fitAll`)
- [ ] 🟢 배치 애니메이션(트윈)
- [ ] 🟢 정렬 기준별 재배치 (이름/추가순/경로/랜덤, 재실행 시 역순 토글)

### 1.6 삭제 / 복제 `[x]`
- [x] 🔴 `Del`/`Backspace` 선택 삭제 (board.items + 스프라이트 + 선택 정리)
- [x] 🔴 `scene.removeImage`가 `sprite.destroy()`로 텍스처 해제
- [x] 🔴 `Ctrl+D` 복제 (structuredClone + 새 id + offset 24px)
- [x] 🟡 z-order 재계산(삭제 시 빈 z 정리) (`core/zorder.ts` normalizeZ) + `]`/`[`/Shift 앞뒤·맨앞뒤

### 1.7 저장 / 열기 — 브라우저 임시 단계 `[x]`
- [x] 🔴 `serialize(board)` → .refb Blob 다운로드 (`core/io.ts` `saveBoard`, saver 작성)
- [x] 🔴 파일 열기 → FileReader → deserialize → `scene.rebuild` 재구성 (`loadBoardFile`/`pickRefbFile`)
- [x] 🔴 data URL 임베드라 단일 파일로 완결
- [x] 🔴 `Ctrl+S` / `Ctrl+O` 바인딩 (preventDefault)
- [ ] 🟡 저장 안 된 변경 표시(타이틀 `*`) + 새로고침 경고(beforeunload)
- [ ] 🟡 (Phase 4) Tauri fs API로 진짜 파일 저장·열기 교체

### 1.8 Undo / Redo `[x]`
- [x] 🟡 방식: 보드 상태 스냅샷(structuredClone) — `core/history.ts` `History` (historian 작성)
- [x] 🟡 변경 직전 push (상한 200), **current 추적으로 redo 정확성 보장**(통합 중 버그 수정)
- [x] 🟡 `Ctrl+Z` / `Ctrl+Shift+Z`(=`Ctrl+Y`)
- [x] 🟡 드래그 이동은 첫 실제 이동 시 1커밋(클릭만은 제외)

### ✅ Phase 1 완료 기준(DoD)
- [x] 이미지 여러 장 → 선택·이동·자동패킹 → 삭제/복제 → Undo/Redo 동작(eval+screenshot 검증)
- [~] `.refb` 저장 → 새로고침 후 열기 (코드·tsc PASS, 실파일 왕복은 수동 확인 권장)
- [x] 콘솔 에러 0, **tsc 타입 에러 0**

---

## Phase 2 — 편집 도구 (v1.0) 🟡 `[ ]`

### 2.1 변형 기즈모 (스케일 / 회전) `[~]`
- [x] 🟡 선택 항목에 변형 기즈모 렌더 (모서리/변중점/회전 핸들, `core/gizmo.ts` + `scene.drawGizmo`)
- [x] 🟡 모서리/변 드래그 스케일 (반대편 피벗 고정, 균등)
- [x] 🟡 중심 기준 스케일 (Alt 누르고 드래그)
- [x] 🟡 회전 (회전 핸들 드래그), 45° 스냅 (Shift)
- [ ] 🟡 다중 선택 개별 중심 변형 — 현재 단일 선택만(추후)
- [x] 🟡 핸들 크기 화면 고정(줌 보정)

### 2.2 자르기 (Crop) — 비파괴 `[~]`
- [x] 🟡 crop 사각형 데이터 모델 추가 (`BoardImage.crop`, 원본픽셀 기준)
- [x] 🟡 텍스처 frame으로 크롭 렌더 (`core/crop.ts` cropToFrame + `scene.applyCrop`, 공유 source 유지)
- [x] 🟡 크롭 모드 진입(`C`)/영역 드래그 지정(회전·flip·기존crop 역변환, 크롭 후 제자리 유지) — 내부 팬·줌은 추후
- [x] 🟡 크롭 리셋(`Ctrl+Shift+C`) / 변형 리셋(`Ctrl+Shift+T`) 분리

### 2.3 뒤집기 / 보간 `[~]`
- [x] 🟡 좌우/상하 뒤집기 (`Alt+Shift+H`/`V`, transform.flipX/Y 부호, 비파괴)
- [ ] 🟢 픽셀 보간 토글 (bilinear ↔ nearest, `texture.source.scaleMode`)

### 2.4 정렬 / 분배 / 정규화 `[~]`
- [x] 🟡 가장자리 정렬 (상/하/좌/우, `Ctrl+화살표`) + 중심정렬(hcenter/vcenter, `core/align.ts`)
- [x] 🟡 균등 분배 (수평/수직, `Ctrl+Shift+화살표`)
- [x] 🟢 크기 정규화 (너비/높이/스케일, `normalizeSize`) — 면적은 추후

### 2.5 레이어 순서 (z-order) `[~]`
- [x] 🟡 맨 앞/뒤·한 단계 (`]`/`[`/Shift), zIndex 갱신 + board 동기화 (`core/zorder.ts`, Phase 1.6에서 구현)
- [ ] 🟡 (주의) 캔버스 z-order ≠ OS 창 스태킹 — 별도 관리

### 2.6 그룹 `[x]`
- [x] 🟢 그룹 데이터 모델 (`BoardImage.groupId` — 별도 유니온 대신 필드 방식, 직렬화·이동 단순)
- [x] 🟢 그룹/해제 (`Ctrl+G` / `Ctrl+Shift+G`), 멤버 클릭·러버밴드 시 그룹 통째 선택(`core/group.ts` expandByGroup) → 그룹 단위 이동은 선택 확장으로 자동

### 2.7 속성 `[~]`
- [x] 🟡 투명도 조절 (드래그) — 우상단 슬라이더(`core/opacity-control.ts`), 실시간 미리보기 + 놓을 때 undo 1스텝
- [x] 🟡 잠금 토글 (`Alt+L`) — 이동/변형 차단, 외곽선 주황 구분
- [ ] 🟢 그레이스케일 (개별 / 캔버스 전체)
- [ ] 🟢 코멘트 / 소스 열기

### 2.8 그리드 & 스냅 `[x]`
- [x] 🟡 배경 그리드 렌더 (`G` 토글, `core/grid.ts` 적응형 1-2-5 간격 + major/minor, scene 최하단 gridLayer)
- [x] 🟡 그리드 스냅 (표시와 독립 동작) — 스냅은 `N`(snap.ts), 그리드 표시는 `G`로 분리됨

### 2.9 노트 / 텍스트
- [ ] 🟢 텍스트 노트 아이템 (`BoardNote`, 리치 텍스트)
- [ ] 🟢 이동·회전·크기, 색/정렬/목록/링크
- [ ] 🟢 드로잉 모드 (펜/도형/지우개)

### 2.10 색상 & 측정
- [ ] 🟡 색상 추출 스포이드 (RGB/HSV/HEX, `S+클릭`)
- [ ] 🟢 이미지 좌표/치수 표시 (`D+클릭`)

---

## Phase 3 — 파일 & 내보내기 (v1.0) 🟡 `[~]`

### 3.1 .refb 정식 포맷
- [x] 🟡 ZIP 컨테이너 (`board.json` + `assets/` + 썸네일) — fflate 사용 (`core/refb.ts`)
- [x] 🟡 이미지 임베드 ↔ 링크 모드 토글 (packRefb `mode` 옵션)
- [x] 🟡 원격/스크린샷 이미지는 항상 임베드 규칙
- [x] 🟡 포맷 버전 필드 상위 호환 (manifest `container` 버전 + 평문 JSON 하위호환)
- [ ] 🟢 PureRef `.pur` 가져오기 (참고: github.com/FyorDev/PureRef-format)

### 3.2 자동 저장 / 복구
- [x] 🟡 주기적 저장 (기본 5분, 설정 가능) — `core/autosave.ts` IndexedDB 스냅샷
- [x] 🟡 임시 파일 기반 크래시 복구 (부팅 시 복구 프롬프트 → restore)
- [x] 🟡 저장 안 된 씬 처리 정책 (묻기/버리기/자동) — 복구 confirm + dirty beforeunload 경고

### 3.3 내보내기 (Export)
- [x] 🟡 씬 전체 → 단일 이미지 (`app.renderer.extract`) — Ctrl+E
- [x] 🟡 선택 → 단일 이미지 — Ctrl+Shift+E
- [~] 🟡 전체/선택 → 개별 파일 (`exportEach` 모듈 준비, 단축키 미배선)
- [~] 🟡 포맷 PNG/JPG, 크롭 적용 토글 (모듈 jpg 지원·크롭 자동반영, UI 토글 미배선)
- [ ] 🟢 파일명 템플릿(`%원본/순번/씬명`), 해상도/배율 (scale 옵션은 존재)

### 3.4 최근 파일 / 시작
- [~] 🟡 최근 파일 목록 (개수 설정) — `core/recent.ts` 데이터계층 완료, 목록 UI 미배선
- [x] 🟡 시작 시 마지막 씬 자동 열기 (getLastSession 부팅 복원)

---

## Phase 4 — Tauri 데스크탑 셸 🟡 `[~]`

> 웹앱(app/)을 Tauri로 감싸 네이티브 데스크탑 기능 부여. **Rust 설치 선행(0.1).**

### 4.1 Tauri 셋업
- [x] 🟡 `src-tauri/` 추가 (`tauri init`, Tauri 2.11), Vite 1420 연동 (devUrl·strictPort 일치)
- [x] 🟡 dev/build 파이프라인 (`tauri dev` / `tauri build`) — cargo build·vite build 검증 통과
- [x] 🟡 앱 아이콘 / 메타데이터 (기본 아이콘셋, productName=RefBoard, identifier=com.refboard.app)

### 4.2 네이티브 파일 I/O
- [x] 🟡 `.refb` 파일 열기/저장 (Tauri fs + dialog plugin) — `tauri-bridge.ts`, isDesktop() 분기·웹 폴백 유지
- [ ] 🟡 파일 연결(.refb 더블클릭으로 열기, Windows) — 후속
- [x] 🟡 드래그앤드롭 OS 파일 경로 처리 — `onOsFileDrop`→`readDroppedFile`→importFiles(데스크탑 이미지 경로). 파일연결(.refb 더블클릭)은 후속

### 4.3 윈도우 모드 — PureRef 정체성
- [x] 🟡 항상 위 (`Ctrl+Shift+A`, `setAlwaysOnTop`)
- [~] 🟢 특정 앱 위에만 / 항상 아래 — 항상 아래 `Ctrl+Shift+B`(setAlwaysOnBottom·항상위와 배타) 완료 · 특정 앱 위는 후속
- [x] 🟡 마우스 통과 click-through (`Ctrl+Alt+T`, `setIgnoreCursorEvents`) — 키보드로 해제 가능
- [ ] 🟢 오버레이 모드 (이미지별 개별 창 — 난이도 높음, 후순위)
- [x] 🟡 창 불투명도 조절 (`Ctrl+Shift+O` 순환 100→85→65→40) — Rust 커스텀 커맨드 `set_window_opacity`(windows crate SetLayeredWindowAttributes), cargo check 통과
- [~] 🟡 미니멀 UI (타이틀바 숨김: `Ctrl+Shift+D`, `setDecorations`) — 호버 모드는 후속
- [x] 🟢 창/캔버스 잠금 (`Ctrl+Shift+L`) — onPointerDown 가드로 편집 차단(팬/줌/단축키는 유지)

### 4.4 UI 셸 & 테마
- [x] 🟡 툴바 / 상태바 / 설정 패널 — `toolbar.ts`(상단 툴바+하단 상태바: 줌/커서/선택/총수) · `settings-panel.ts`(테마·단축키 탭 모달)
- [x] 🟡 테마 프리셋 (Dark/Light/Glass) + 커스텀 — `theme.ts`, --rb-* CSS 변수·캔버스 0xRRGGBB 연동
- [x] 🟢 UI 요소별 색상 커스터마이징 — 설정 패널 테마 탭에서 `--rb-*` 토큰별 컬러피커 오버라이드

### 4.5 단축키 & 커맨드 팔레트
- [x] 🟡 전역 단축키 매핑 시스템 (재바인딩 가능) — `keymap.ts`, 40개 액션·localStorage diff 저장·matchKey
- [x] 🟡 커맨드 팔레트 (`Ctrl+Shift+P`) — `command-palette.ts`, 부분일치 검색·↑↓ 네비·Esc 닫기
- [x] 🟢 단축키 설정 UI — 설정 패널 단축키 탭(액션별 재바인딩·충돌 감지·기본값 리셋)

### 4.6 성능 & 빌드
- [~] 🟡 성능 모드 (메모리/디스크 캐시), 대형 이미지 자동 다운스케일 — 다운스케일 `downscale.ts`(긴 변 4096) + 가상화 `virtualize.ts`(GPU 텍스처 언로드) 완료, 디스크 캐시는 후속
- [x] 🟡 100~500장 스트레스 테스트 (가상화/언로드) — `virtualize.ts` createVirtualizer(히스테리시스 margin)+spawnStressImages/measureFps, applyCam에서 virt.update
- [~] 🟡 Windows 빌드/서명 — 디버그 컴파일 검증, 릴리스 빌드·서명은 후속

---

## Phase 5 — 웹 공유 (차별점) 🟢 `[ ]`

> 사용자 핵심 요구. `core-canvas`를 읽기 전용 웹 뷰어로 재사용. 단계별로 서버 의존도 상승.

### 5.0 사전 설계
- [ ] 🟢 BaaS 선정 (Supabase 권장: Auth+DB+Storage+Realtime 통합)
- [ ] 🟢 보드 직렬화 스키마 = 로컬 `.refb`와 동일 확인 (재사용 핵심)

### 5.1 업로드 & 링크 (읽기 전용)
- [ ] 🟢 "웹에 공유" → 보드 JSON + 이미지 업로드 (Storage)
- [ ] 🟢 고유 단축 링크 발급 (`/b/xxxxxx`)
- [ ] 🟢 다중 해상도 이미지 생성 (thumb/medium/orig, WebP)
- [ ] 🟢 공유 갱신(재업로드 같은 링크)

### 5.2 웹 뷰어 (`web-viewer/`)
- [ ] 🟢 `core-canvas` 재사용한 읽기 전용 뷰어 (편집 비활성)
- [ ] 🟢 줌/팬 + fit + 개별 포커스
- [ ] 🟢 이미지 라이트박스 (풀스크린, 다음/이전)
- [ ] 🟢 보드 제목/설명/작성자 표시

### 5.3 모바일 / PWA
- [ ] 🟢 반응형 레이아웃 + 터치 제스처(핀치 줌/드래그 팬)
- [ ] 🟢 저해상도 우선 로딩 + lazy-load + 뷰포트 밖 언로드
- [ ] 🟢 PWA (홈 화면 추가, 서비스 워커 오프라인 캐싱)

### 5.4 권한 & 보안
- [ ] 🟢 공개 / 비공개 토큰 / 비밀번호 / 만료
- [ ] 🟢 다운로드 허용·금지, 워터마크(선택)
- [ ] 🟢 HTTPS + 비공개 버킷 + 서명 URL
- [ ] 🟢 업로드 약관(저작권 책임 명시)

### 5.5 협업 (확장)
- [ ] 🟢 이미지 댓글 / 위치 핀 (BaaS DB write)
- [ ] 🟢 실시간 동기화 (데스크탑 수정 → 웹 자동 반영, Realtime/WebSocket)
- [ ] ⚪ 동시 커서 (Figma식)
- [ ] 🟢 임베드 코드 (`<iframe>`)

---

## Phase 6 — 품질 & 마감 `[ ]`

- [ ] 🟡 전체 기능 버그 점검 (canvas-photo-editor식 P1/P2/P3 감사)
- [ ] 🟡 성능 프로파일링 (다수 이미지 메모리/프레임)
- [ ] 🟢 접근성 (웹 뷰어 키보드 탐색·대체 텍스트)
- [ ] 🟢 다국어 (한/영 우선)
- [ ] 🟢 사용 설명서 / 단축키 도움말
- [ ] 🟡 배포 패키징 + 버전 태깅

---

## 부록 — 현재 코드 맵
| 파일 | 역할 | 상태 |
|---|---|---|
| `app/src/core/board.ts` | 보드 상태 모델(SSOT) · 직렬화 | ✅ 기초 |
| `app/src/core/scene.ts` | PixiJS 렌더러 · 카메라 · 포인터이벤트 · 선택오버레이 · AABB | ✅ |
| `app/src/main.ts` | 입력 배선(선택/이동/러버밴드/줌/팬/드롭/단축키) | ✅ |
| `app/src/core/selection.ts` | 선택 상태 집합 + 변경 구독 | ✅ (1.3) |
| `app/src/core/pack.ts` | 자동 패킹(Skyline bin packing) | ✅ (1.5) |
| `app/src/core/io.ts` | .refb 저장/열기 | ✅ (1.7) |
| `app/src/core/history.ts` | Undo/Redo(상태 스냅샷, current 추적) | ✅ (1.8) |
| `app/src/core/zorder.ts` | z순서(맨앞뒤/한단계/normalizeZ) | ✅ (1.6) |
| `app/src/core/minimap.ts` | 미니맵 오버레이(M 토글·클릭 점프) | ✅ (1.1) |
| `app/src/core/snap.ts` | 그리드/이웃 스냅(N 토글) | ✅ (1.4) |
| `app/src/core/gizmo.ts` | 변형 기즈모 | ⬜ 예정 (2.1) |
| `src-tauri/` | Tauri 데스크탑 셸 | ⬜ 예정 (4.x) |
| `web-viewer/` | 읽기 전용 웹 뷰어 | ⬜ 예정 (5.2) |
