# RefBoard 개발 체크리스트

> 이 문서는 RefBoard 개발 진행의 **단일 출처(SSOT)** 입니다. 작업이 끝날 때마다 `[ ]` → `[x]`로 갱신합니다.
> 상세 기능 근거는 [기능명세서.md](기능명세서.md) 참조.
> 최종 갱신: 2026-06-28 (Phase 7.5 버그 감사 후속 추가)

## 범례
- `[x]` 완료 · `[ ]` 예정 · `[~]` 진행/부분완료 · `🆕` = 2026-06-21~22 추가
- 🔴 **P0** MVP 필수 · 🟡 **P1** v1.0 · 🟢 **P2** 확장 · ➖ 설계상 해당없음
- `(파일/함수)` = 구현 위치 힌트

## 진행 현황 요약
| Phase | 상태 | 비고 |
|---|---|---|
| 0. 셋업 & 인프라 | `[x]` ~95% | git·README·공개 repo·Rust 툴체인(Tauri 빌드 가동) ✅. 잔여=ESLint/Prettier |
| 1. 캔버스 코어 (MVP) | `[x]` 100% | 핵심+소품 전부 ✅ + 🆕 이전/다음 순회. 잔여=좌표HUD(🟢)뿐 |
| 2. 편집 도구 | `[x]` ~95% | 기즈모·크롭·뒤집기·정렬/분배/정규화(+면적)·중앙정렬·격자기준정렬·z순서·그룹·투명도·잠금·그리드·드로잉·스포이드 ✅. 잔여=리치텍스트노트·픽셀보간·그레이스케일(🟢) |
| 3. 파일 & 내보내기 | `[x]` ~95% | .refb·자동저장/복구·씬/선택/개별 내보내기(PNG/JPG/BMP)·최근파일UI ✅. 잔여=.pur·파일명템플릿(🟢) |
| 4. Tauri 데스크탑 셸 | `[~]` ~90% | 셋업·빌드·파일IO·윈도우모드(항상위/아래·클릭스루·불투명도·타이틀바·캔버스잠금)·테마·단축키·팔레트·가상화 ✅. 잔여=특정앱위·오버레이·성능4단계·릴리스서명 |
| 5. 웹 공유 (차별점) | `[~]` ~80% | **Supabase 실배포(refboard-e0m.pages.dev)**·업로드/링크/뷰어·라이트박스·PWA·다중해상도·권한(공개/만료/이메일) ✅. 잔여=비밀번호·재업로드·실시간동기화 |
| 6. 품질 & 마감 | `[~]` ~70% | 감사 2회(06-18 59건·06-21 9건) 수정·성능·접근성·README단축키표 ✅. 잔여=다국어·패키징 |
| 7. 개선 백로그 (06-26 감사) | `[~]` ~85% | 🔴견고성/테스트(7.1·4건)✅ · 🟠성능/접근성/정리(7.2·9건)✅ · 🟡폴리시(커서·모달·빈캔버스버튼)✅ · 1차 검수후속 4건✅ · 7.5 핵심 3건✅. 잔여=main.ts God-file 분리(L) · 7.5 잔여(뷰어/라이트박스/다탭·P3) |

---

## Phase 0 — 프로젝트 셋업 & 인프라 `[x]`

### 0.1 개발 환경
- [x] Node / npm / bun / WebView2 확인 (Node 24.13, npm 11.6, bun 1.3)
- [x] 스택 확정: Tauri 2 + Vite + TypeScript + PixiJS(WebGL)
- [x] 🟡 Rust 툴체인 설치 (Tauri 빌드 가동 확인 — rustc 1.96 / stable-msvc)
- [x] 🟡 MSVC C++ Build Tools 확인 (VS2022, link.exe — Tauri 빌드 통과)
- [x] 🟡 Tauri CLI 설치 (`@tauri-apps/cli` 2.11)

### 0.2 프로젝트 구조
- [x] `app/` Vite + TS + PixiJS 스캐폴딩 (package.json, tsconfig, vite.config, index.html)
- [x] `npm install` 완료
- [x] `src/core/` (재사용 엔진) ↔ `src/` (앱 입력) 폴더 분리
- [x] dev 서버 등록 (`~/.claude/launch.json` 'refboard' 포트 1420)
- [x] 🔴 `.gitignore` (node_modules, dist, src-tauri/target)
- [x] 🟡 `git init` + 첫 커밋 (로컬 user.email=fkrn75@gmail.com) → 공개 repo **github.com/fkrn75/RefBoard**
- [x] 🟡 `README.md` (실행법, 구조 설명, 🆕 단축키표)
- [ ] 🟢 ESLint + Prettier (코드 스타일 통일)
- [ ] 🟢 npm audit 취약점 2건 점검

---

## Phase 1 — 캔버스 코어 (MVP) 🔴 `[x]`

> 목표: 설치 없이 브라우저에서 "이미지를 모으고·배치하고·저장"하는 최소 동작 보드.

### 1.1 캔버스 & 카메라 `[x]`
- [x] PixiJS Application 초기화 (`scene.ts` `Scene.create`)
- [x] world 컨테이너 1개를 이동/스케일 (무한 캔버스)
- [x] 휠 줌 (커서 위치 고정점) (`main.ts` wheel)
- [x] 우클릭/휠클릭 드래그 팬 (`main.ts` pointerdown/move/up)
- [x] 우클릭 컨텍스트 메뉴 차단
- [x] 창 리사이즈 자동 대응 (`resizeTo: host`)
- [x] 🔴 줌 한계 클램프(0.05~20) + `Ctrl+0` 줌 100% 리셋 (`main.ts` zoomReset)
- [x] 🔴 전체 보기 fit-all (`Ctrl+Space`) — `scene.contentBounds` AABB 후 카메라 맞춤
- [x] 🔴 이미지 포커스 fit (`Space` / 더블클릭) — 선택 이미지에 줌 (`focusSelected`)
- [x] 🟡 이전/다음 항목 순회 (`←`/`→`, 선택 이동 + 포커스, `navigateItem`) 🆕
- [x] 🟡 카메라 상태를 board.camera에 영구 동기화 (`applyCam`에서 갱신)
- [ ] 🟢 좌표/줌 레벨 인디케이터(HUD)
- [x] 🟢 미니맵 (`core/minimap.ts` Minimap, `M` 토글 + 클릭 점프)

### 1.2 이미지 가져오기 `[x]`
- [x] 드래그앤드롭 (이미지 파일 필터, 드롭 위치=월드 좌표) (`main.ts` drop)
- [x] 클립보드 붙여넣기 `Ctrl+V` (화면 중앙 배치) (`main.ts` paste)
- [x] 여러 파일 일괄 드롭 (offset으로 어긋나게)
- [x] data URL 임베드 + 원본 크기(natural) 측정 + 🆕 파일명(name)·추가시각(addedAt) 기록(격자정렬용)
- [x] 🔴 파일 열기 다이얼로그 `Ctrl+I` (`openImageFiles`, `<input type=file multiple>`)
- [x] 🔴 로딩 인디케이터 (`importFiles` N/M 진행 표시 오버레이)
- [x] 🔴 디코드 실패/비이미지 에러 처리 + 토스트 알림 (`importFiles` 사전 검증→실패 건너뜀)
- [x] 🟡 GIF 애니메이션 표시 (`@pixi/gif` AnimatedGIF.fromBuffer, `scene.addImage` 분기)
- [ ] 🟡 URL 붙여넣기 → 페이지 이미지 스크랩 (웹 CORS 차단 → Tauri Rust fetch/프록시 결정 필요, **defer**)
- [ ] 🟢 내장 스크린샷 캡처 (Tauri 단계)
- ➖ 누락 이미지 Relink — dataURL 임베드 구조라 끊긴 경로 자체가 없음(링크 모드 선구현 필요 시 재검토)

### 1.3 선택 (Selection) `[x]`
- [x] 🔴 `selectedIds: Set<string>` 선택 상태 모듈 (`core/selection.ts` — Selection 클래스 + onChange 구독)
- [x] 🔴 스프라이트 hit-test (stage 전역 포인터 이벤트 + `e.target.label`로 id 식별)
- [x] 🔴 좌클릭 단일 선택 → selectedIds 교체
- [x] 🔴 선택 시각화: 외곽선 박스 (`scene.drawSelection`, 줌 보정 오버레이 레이어)
- [x] 🔴 Shift+좌클릭 토글 다중 선택
- [x] 🔴 빈 캔버스 좌클릭 → 선택 해제
- [x] 🔴 빈 곳 좌클릭 드래그 → 러버밴드(반투명 Graphics) → 교차 항목(AABB) 선택
- [x] 🔴 `Ctrl+A` 전체 선택 / `Esc` 해제
- [x] 🟡 경계상자 계산 (`scene.getItemAABB`/`corners`, 회전·스케일 고려)
- [x] 🟡 잠긴(locked) 항목 — 이동/기즈모 변형 차단, 선택 외곽선 주황 구분

### 1.4 이동 (Move) `[x]`
- [x] 🔴 선택 스프라이트 위 좌클릭 드래그 → 이동
- [x] 🔴 월드 좌표 delta로 `transform.x/y` 갱신 (줌 자동 보정)
- [x] 🔴 다중 선택 시 동일 delta 일괄 이동
- [x] 🔴 이동 중 `board.items[].transform` 동기화 (SSOT)
- [x] 🟡 커서 `grab` → `grabbing` 전환 (`scene.setCursor`, 드래그 동안)
- [x] 🟡 Shift 누르면 수평/수직 축 고정 이동
- [x] 🟢 이웃 가장자리 스냅 / 그리드 스냅 (`core/snap.ts`, `N` 토글·이웃 우선→그리드)

### 1.5 자동 패킹 (Arrange optimal) `[x]` — ⭐ 킬러 기능
- [x] 🔴 패킹 알고리즘 — Skyline bin packing (`core/pack.ts` `packImages`)
- [x] 🔴 목표 영역 종횡비 = 창 비율(host.clientWidth/Height)
- [x] 🔴 각 이미지 실제 크기(natural × scale) 수집
- [x] 🔴 패킹 결과 좌표를 `transform`에 적용(뷰 중앙 평행이동)
- [x] 🔴 항목 간 간격 padding (기본 16px)
- [x] 🔴 `Ctrl+P` 바인딩 (선택 2개+면 선택만, 아니면 전체)
- [x] 🟡 빈 보드 / 1장 예외 처리 (pack.ts 내장)
- [x] 🟡 패킹 후 fit-all 자동 호출 (`packAll` 끝 `fitAll`)
- [x] 🟡 캔버스 최적화(정돈) — `Ctrl+Alt+P` packAll+fitAll (`optimizeCanvas`) 🆕
- [x] 🟡 정렬 기준별 재배치 (이름/추가순/레이어순/무작위, 재실행 시 역순 토글) — `core/arrange-sort.ts` arrangeGrid, `Ctrl+Alt+N/A/O/R` 🆕 (경로순은 dataURL 임베드라 제외)
- [ ] 🟢 배치 애니메이션(트윈)

### 1.6 삭제 / 복제 `[x]`
- [x] 🔴 `Del`/`Backspace` 선택 삭제 (board.items + 스프라이트 + 선택 정리)
- [x] 🔴 `scene.removeImage`가 `sprite.destroy()`로 텍스처 해제
- [x] 🔴 `Ctrl+D` 복제 (structuredClone + 새 id + offset 24px)
- [x] 🟡 z-order 재계산(삭제 시 빈 z 정리) (`core/zorder.ts` normalizeZ) + `]`/`[`/Shift 앞뒤·맨앞뒤

### 1.7 저장 / 열기 `[x]`
- [x] 🔴 `serialize(board)` → .refb Blob 다운로드 (`core/io.ts` `saveBoard`)
- [x] 🔴 파일 열기 → FileReader → deserialize → `scene.rebuild` 재구성 (`loadBoardFile`/`pickRefbFile`)
- [x] 🔴 data URL 임베드라 단일 파일로 완결
- [x] 🔴 `Ctrl+S` / `Ctrl+O` 바인딩 (preventDefault)
- [x] 🟡 저장 안 된 변경 표시 + 새로고침 경고(beforeunload, dirty 추적)
- [x] 🟡 (Phase 4) Tauri fs API로 진짜 파일 저장·열기 (`tauri-bridge.ts`, isDesktop 분기·웹 폴백)

### 1.8 Undo / Redo `[x]`
- [x] 🟡 방식: 보드 상태 스냅샷(structuredClone) — `core/history.ts` `History`
- [x] 🟡 변경 직전 push (상한 200), current 추적으로 redo 정확성 보장
- [x] 🟡 `Ctrl+Z` / `Ctrl+Shift+Z`(=`Ctrl+Y`)
- [x] 🟡 드래그 이동은 첫 실제 이동 시 1커밋(클릭만은 제외)

---

## Phase 2 — 편집 도구 (v1.0) 🟡 `[x]`

### 2.1 변형 기즈모 (스케일 / 회전) `[x]`
- [x] 🟡 선택 항목에 변형 기즈모 렌더 (모서리/변중점/회전 핸들, `core/gizmo.ts` + `scene.drawGizmo`)
- [x] 🟡 모서리/변 드래그 스케일 (반대편 피벗 고정, 균등)
- [x] 🟡 중심 기준 스케일 (Alt 누르고 드래그)
- [x] 🟡 회전 (회전 핸들 드래그), 45° 스냅 (Shift)
- [ ] 🟡 다중 선택 개별 중심 변형 — 현재 단일 선택만(추후)
- [x] 🟡 핸들 크기 화면 고정(줌 보정)

### 2.2 자르기 (Crop) — 비파괴 `[x]`
- [x] 🟡 crop 사각형 데이터 모델 (`BoardImage.crop`, 원본픽셀 기준)
- [x] 🟡 텍스처 frame으로 크롭 렌더 (`core/crop.ts` cropToFrame + `scene.applyCrop`)
- [x] 🟡 크롭 모드 진입(`C`)/영역 드래그 지정(회전·flip·기존crop 역변환) — 내부 팬·줌은 추후
- [x] 🟡 크롭 리셋(`Ctrl+Shift+C`) / 변형 리셋(`Ctrl+Shift+T`) 분리

### 2.3 뒤집기 / 보간 `[~]`
- [x] 🟡 좌우/상하 뒤집기 (`Alt+Shift+H`/`V`, transform.flipX/Y 부호, 비파괴)
- [ ] 🟢 픽셀 보간 토글 (bilinear ↔ nearest, `texture.source.scaleMode`)

### 2.4 정렬 / 분배 / 정규화 `[x]`
- [x] 🟡 가장자리 정렬 (상/하/좌/우, `Ctrl+화살표`) + 중앙정렬(가로/세로, 커맨드 팔레트, `core/align.ts`) 🆕 중앙정렬 runAction 배선
- [x] 🟡 균등 분배 (수평/수직, `Ctrl+Shift+화살표`)
- [x] 🟡 크기 정규화 (너비/높이/배율/**면적**, `normalizeSize`, `Ctrl+Alt+화살표`) 🆕 면적 모드 추가
- [ ] 🟢 쌓기(Stack) / 정렬 간격(padding) UI

### 2.5 레이어 순서 (z-order) `[x]`
- [x] 🟡 맨 앞/뒤·한 단계 (`]`/`[`/Shift), zIndex 갱신 + board 동기화 (`core/zorder.ts`)
- [x] 🟡 (주의) 캔버스 z-order ≠ OS 창 스태킹 — 별도 관리

### 2.6 그룹 `[x]`
- [x] 🟢 그룹 데이터 모델 (`BoardImage.groupId` — 필드 방식, 직렬화·이동 단순)
- [x] 🟢 그룹/해제 (`Ctrl+G` / `Ctrl+Shift+G`), 멤버 클릭·러버밴드 시 그룹 통째 선택(`core/group.ts` expandByGroup)

### 2.7 속성 `[~]`
- [x] 🟡 투명도 조절 (드래그) — 우상단 슬라이더(`core/opacity-control.ts`), 실시간 미리보기 + undo 1스텝
- [x] 🟡 잠금 토글 (`Alt+L`) — 이동/변형 차단, 외곽선 주황 구분
- [~] 🟢 코멘트(이미지 댓글 = 편집기 메모 ✅, `Alt+C`) / 소스 열기(➖ dataURL 임베드라 원본 경로 없음)
- [ ] 🟢 그레이스케일 (개별 / 캔버스 전체)

### 2.8 그리드 & 스냅 `[x]`
- [x] 🟡 배경 그리드 렌더 (`G` 토글, `core/grid.ts` 적응형 1-2-5 간격 + major/minor)
- [x] 🟡 그리드 스냅 (표시와 독립 동작) — 스냅 `N`(snap.ts), 표시 `G`로 분리
- [ ] 🟢 점(dots) 스타일 그리드 (현재 라인만)

### 2.9 노트 / 텍스트 `[~]`
- [~] 🟡 텍스트 노트 아이템 (`BoardNote`, 평문+색·크기·글꼴 ✅ / 리치서식 굵게·목록·링크 ⬜ = **defer: PixiJS HTML 렌더 재설계 필요**) — `T` 도구
- [x] 🟢 이동·회전·크기(기즈모 자동 편입), 색·크기·글꼴 변경 UI (`core/style-control.ts`)
- [x] 🟢 드로잉 모드 (펜/직선/사각형/타원/지우개, `P/L/R/O/E` 도구)

### 2.10 색상 & 측정 `[~]`
- [x] 🟡 색상 추출 스포이드 (`S`, `window.EyeDropper`+캔버스 폴백, HEX 복사) — `core/eyedropper.ts` 🆕
- [ ] 🟢 이미지 좌표/치수 표시 (`D+클릭`)

---

## Phase 3 — 파일 & 내보내기 (v1.0) 🟡 `[x]`

### 3.1 .refb 정식 포맷 `[x]`
- [x] 🟡 ZIP 컨테이너 (`board.json` + `assets/` + 썸네일) — fflate (`core/refb.ts`)
- [x] 🟡 이미지 임베드 ↔ 링크 모드 토글 (packRefb `mode` 옵션)
- [x] 🟡 원격/스크린샷 이미지는 항상 임베드 규칙
- [x] 🟡 포맷 버전 필드 상위 호환 (manifest `container` 버전 + 평문 JSON 하위호환)
- [ ] 🟢 PureRef `.pur` 가져오기 (참고: github.com/FyorDev/PureRef-format)

### 3.2 자동 저장 / 복구 `[x]`
- [x] 🟡 주기적 저장 (기본 5분) — `core/autosave.ts` IndexedDB 스냅샷
- [x] 🟡 임시 파일 기반 크래시 복구 (부팅 시 복구 프롬프트 → restore)
- [x] 🟡 저장 안 된 씬 처리 정책 — 복구 confirm + dirty beforeunload 경고

### 3.3 내보내기 (Export) `[x]`
- [x] 🟡 씬 전체 → 단일 이미지 (`app.renderer.extract`) — `Ctrl+E`
- [x] 🟡 선택 → 단일 이미지 — `Ctrl+Shift+E`
- [x] 🟡 전체/선택 → 개별 파일 (`exportEach`, `Ctrl+Alt+I`/팔레트, 파일명=원본명+순번) 🆕 배선
- [x] 🟡 포맷 PNG/JPG/**BMP** (BMP 무압축 인코더 직접구현), 크롭 자동반영 🆕 BMP 추가
- [ ] 🟢 파일명 템플릿(`%원본/순번/씬명`), 해상도/배율 (scale 옵션은 존재)

### 3.4 최근 파일 / 시작 `[x]`
- [x] 🟡 최근 파일 목록 + 열기 UI (`core/recent-picker.ts` 모달, `Ctrl+Alt+L`) 🆕 목록 UI 추가
- [x] 🟡 시작 시 마지막 씬 자동 열기 (getLastSession 부팅 복원)

---

## Phase 4 — Tauri 데스크탑 셸 🟡 `[~]`

### 4.1 Tauri 셋업 `[x]`
- [x] 🟡 `src-tauri/` 추가 (Tauri 2.11), Vite 1420 연동 (devUrl·strictPort 일치)
- [x] 🟡 dev/build 파이프라인 (`tauri dev` / `tauri build`) — cargo build·vite build 통과
- [x] 🟡 앱 아이콘 / 메타데이터 (productName=RefBoard, identifier=com.refboard.app)

### 4.2 네이티브 파일 I/O `[x]`
- [x] 🟡 `.refb` 파일 열기/저장 (Tauri fs + dialog plugin) — `tauri-bridge.ts`, isDesktop() 분기·웹 폴백
- [x] 🟡 드래그앤드롭 OS 파일 경로 처리 — `onOsFileDrop`→`readDroppedFile`→importFiles
- [ ] 🟡 파일 연결(.refb 더블클릭으로 열기, Windows) — 후속

### 4.3 윈도우 모드 — PureRef 정체성 `[~]`
- [x] 🟡 항상 위 (`Ctrl+Shift+A`, `setAlwaysOnTop`)
- [x] 🟢 항상 아래 (`Ctrl+Shift+B`, setAlwaysOnBottom·항상위와 배타) — 특정 앱 위에만은 후속
- [x] 🟡 마우스 통과 click-through (`Ctrl+Alt+T`, `setIgnoreCursorEvents`)
- [x] 🟡 창 불투명도 조절 (`Ctrl+Shift+O` 순환) — Rust 커맨드 `set_window_opacity`(windows crate)
- [x] 🟡 미니멀 UI (타이틀바 숨김: `Ctrl+Shift+D`, `setDecorations`) — 호버 모드는 후속
- [x] 🟢 창/캔버스 잠금 (`Ctrl+Shift+L`) — onPointerDown 가드(팬/줌/단축키는 유지)
- [ ] 🟢 오버레이 모드 (이미지별 개별 창) — 난이도 높음, 후순위

### 4.4 UI 셸 & 테마 `[x]`
- [x] 🟡 툴바 / 상태바 / 설정 패널 — `toolbar.ts` · `settings-panel.ts`(테마·단축키 탭)
- [x] 🟡 테마 프리셋 (Dark/Light/Glass) + 커스텀 — `theme.ts`, --rb-* CSS 변수
- [x] 🟢 UI 요소별 색상 커스터마이징 — 설정 패널 테마 탭 토큰별 컬러피커

### 4.5 단축키 & 커맨드 팔레트 `[x]`
- [x] 🟡 전역 단축키 매핑 시스템 (재바인딩 가능) — `keymap.ts`, ~52개 액션·localStorage diff 저장·matchKey
- [x] 🟡 커맨드 팔레트 (`Ctrl+Shift+P`) — `command-palette.ts`, 부분일치·↑↓·Esc
- [x] 🟢 단축키 설정 UI — 설정 패널 단축키 탭(재바인딩·충돌 감지·리셋)

### 4.6 성능 & 빌드 `[~]`
- [x] 🟡 대형 이미지 자동 다운스케일 (`downscale.ts` 긴 변 4096) + 가상화 (`virtualize.ts` GPU 텍스처 언로드)
- [x] 🟡 100~500장 스트레스 테스트 (가상화/언로드, applyCam에서 virt.update)
- [ ] 🟡 성능 모드 4단계 (무캐시/디스크캐시/인메모리/저줌) — **defer: 디스크캐시 등 데스크탑 개념, 웹 부적합(가상화로 대체)**
- [~] 🟡 Windows 빌드/서명 — 디버그 컴파일 검증, 릴리스 빌드·서명은 후속

---

## Phase 5 — 웹 공유 (차별점) 🟢 `[~]`

> ✅ **Supabase 실배포·가동 중** — refboard-e0m.pages.dev (CF Pages + GitHub 자동연동, push→자동재배포). SSOT: `docs/share-backend-design.md`

### 5.0 사전 설계 `[x]`
- [x] 🟢 BaaS 선정·연동 (Supabase) — ShareAdapter 추상화 + SupabaseShareAdapter 실배포(Auth/DB RLS/Storage)
- [x] 🟢 보드 직렬화 스키마 = 로컬 `.refb`와 동일 (뷰어가 deserialize로 동일 스키마 로드)

### 5.1 업로드 & 링크 (읽기 전용) `[x]`
- [x] 🟢 "웹에 공유" → 보드 JSON + 이미지 — 자기완결 HTML(`share-export.ts`) + Supabase 업로드/링크(`supabase-share.ts`)
- [x] 🟢 고유 단축 링크 발급 (`#/b/xxxxxx`) — 데스크탑 `Ctrl+Shift+S`로 발급·클립보드 복사
- [x] 🟢 다중 해상도 이미지 생성 (thumb/medium WebP, `srcset.ts`) — orig 고화질 별도 업로드는 opt-in 후속
- [ ] 🟢 공유 갱신(재업로드 같은 링크) — **defer: 동일 id 덮어쓰기 경로+백엔드 필요**

### 5.2 웹 뷰어 (`viewer.html` + `src/viewer/`) `[x]`
- [x] 🟢 `core`(scene/board) 재사용한 읽기 전용 뷰어 (편집 핸들러 미배선)
- [x] 🟢 줌/팬 + fit (휠 줌·드래그 팬·fitAll)
- [x] 🟢 이미지 라이트박스 (풀스크린, 다음/이전 ‹›·캡션) — `lightbox.ts`
- [~] 🟢 보드 제목/이미지수 표시 (`board-meta.ts`) — 설명/작성자는 후속

### 5.3 모바일 / PWA `[x]`
- [x] 🟢 반응형 레이아웃 + 터치 제스처 — `touch.ts`(핀치 줌/드래그 팬/탭)
- [~] 🟢 저해상도 우선 로딩 — medium 보드뷰 ✅ · `lazyload.ts` IntersectionObserver 뷰어 연결은 후속
- [x] 🟢 PWA (홈 화면 추가, 서비스 워커 오프라인 캐싱) — `manifest.webmanifest`+`sw.js`+`pwa.ts`

### 5.4 권한 & 보안 `[~]`
- [~] 🟢 공개 / 비공개 토큰 / 만료 ✅ (`share-dialog.ts` 공개토글·만료·이메일 허용목록) · 비밀번호 ⬜ · 만료안내화면(`board_block_reason` RPC 코드 완료, **라이브 SQL 사용자 실행 대기**)
- [~] 🟢 다운로드 금지(우클릭 차단만) · 워터마크 ⬜
- [x] 🟢 HTTPS + 비공개 버킷 + 서명 URL (Supabase Storage signed URL)
- [ ] 🟢 업로드 약관(저작권 책임 명시)

### 5.5 협업 (확장) `[~]`
- [~] 🟢 이미지 댓글(편집기 메모 ✅, `Alt+C`) / 위치 핀·웹 협업댓글 ⬜
- [ ] 🟢 실시간 동기화 (데스크탑 수정 → 웹 자동 반영, Realtime/WebSocket)
- [ ] ⚪ 동시 커서 (Figma식)
- [ ] 🟢 임베드 코드 (`<iframe>`)

---

## Phase 6 — 품질 & 마감 `[~]`

> 감사 2회: 06-18 팀오케 59건(P1 14/P2 24/P3 21)→24건 수정, 06-21 전수감사 9건→전건 처리. 상세: `docs/bug-audit-*.md`. 검증: tsc 0·build 0·런타임 에러 0.

- [x] 🟡 전체 기능 버그 점검 (P1/P2/P3 감사 2회) — 코어·IO·공유·뷰어 전건 수정
- [x] 🟡 성능 프로파일링 — applyCam rAF 코얼레싱·미니맵 스킵·scene.rebuild 병렬
- [x] 🟢 접근성 (웹 뷰어 키보드 탐색·대체 텍스트) — 라이트박스 포커스 트랩·alt·대비 AA·PWA 라우팅
- [~] 🟢 사용 설명서 / 단축키 도움말 — README 단축키표 ✅ 🆕 / 인앱 도움말 ⬜
- [ ] 🟢 다국어 (한/영 우선)
- [ ] 🟡 배포 패키징 + 버전 태깅 (릴리스 빌드/서명)

---

## Phase 7 — 개선 백로그 (2026-06-26 4차원 감사) `[~]`

> 데이터 손상 근본수정(84c61ac 전파차단 + 40c2052 origin차단) 완료·검증 후 "개선할점 찾아줘" 요청에 대해
> **견고성·성능·코드품질·UX/a11y 4개 차원을 병렬 에이전트로 감사**한 결과. 코드로 확인된 것만.
> → **2026-06-27 Codex 구현 + Claude 검수로 7.1·7.2 전건 + 7.3 대부분 완료**(빌드 0·테스트 19/19 합격). 잔여는 아래 `[ ]`/`[~]` 참조.
> ⚠️ 우선순위 기호는 **이 감사 자체 기준**(Phase 0~6의 P0/P1/P2와 별개):
> 🔴 **최우선**(견고성/테스트) · 🟠 **중간**(성능/접근성/정리) · 🟡 **폴리시**(빠른 손질).
> 기반은 이미 양호 → 제외: strict 타입(`@ts-ignore`0·`as any`는 eyedropper 격리 6건)·XSS 방어(사용자데이터 전부 textContent)·손상 방어(canDecodeImage·매직바이트·3중 가드)·perf 일부 해결(applyCam rAF 코얼레싱·contentBounds 미니맵 조기종료·rebuild Promise.all).
> 상세 근거: 메모리 `refboard-improvement-backlog-2026-06-26`.

### 7.1 🔴 견고성 · 테스트 (손상수정의 연장선, 최우선)
- [x] 🔴 **테스트 0건 → vitest 도입** (최대 부채) — `*.test.ts`·vitest 의존성 전무, 안전망=tsc+수동뿐. 치명버그가 전부 **순수 로직 불변식**(매직바이트·offset왕복·serialize왕복·AABB)이라 브라우저 없이 단위테스트 가능한데 회귀가드 0 = 같은 손상 재발해도 빌드 통과. 첫 스위트 ROI순: `looksLikeImageBytes`/`canDecodeImage` · `serialize↔deserialize` 왕복(+구버전/손상입력) · `align`/`pack` AABB(natural×scale) · `keymap` combo정규화. *(노력: vitest 도입 S + 스위트 M)*
- [x] 🔴 **`deserialize` 스키마검증 부재** (board.ts:124) = 크래시 클래스 — `JSON.parse(json) as BoardState`만, io.ts는 `schema` 접두사만 검사. 손상/구버전 .refb·복구스냅샷서 items 비배열→`restore()`(main.ts:581)·scene.rebuild "is not iterable", camera 없으면 NaN 전파. refb.ts는 "io.ts가 검증"이라 주석으로 넘기는데 io.ts는 안 함 = **계약 불일치**. 검증헬퍼 1개로 restore·viewer(viewer/main.ts isValidBoard)·recovery·lastSession 일괄. *(노력: M)*
- [x] 🔴 **rebuild `Promise.all`→`allSettled`** (scene.ts:350) — 이미지 1장 디코드 실패가 전체 restore reject → 정상 섞인 보드 통째로 안 열림. addImage에 빈/손상 src 플레이스홀더 폴백도. *(노력: S)*
- [x] 🔴 **조용한 실패 가시화** (데이터손실 무인지) — autosave quota초과 console.warn만(onError UI 미연결) · `setLastSession`/`beforeunload` 빈 catch(recent.ts:83, main.ts:271) · handleUpload try/catch 부재(저장실패=빈목록 조용히 사라짐) · floating promise(`void restore(last)` main.ts:1321 · `void scene.addNote()` 1357=board↔scene desync · viewer `void signIn()`) · 업로드 보상삭제 best-effort 삼킴(supabase-share.ts:209=Storage/DB 고아 과금). 빈 catch에 최소 console.warn + 핵심경로 토스트. *(노력: 각 S)*

### 7.2 🟠 성능 · 접근성 · 정리
- [x] 🟠 **드래그 핫패스 반복 O(n)** (대량보드 끊김) — 이동핸들러 `board.items.find` 루프내 O(m×n)(main.ts:814, 스냅ON 2배) · `lockedIdSet()` 매 pointermove 전체순회(main.ts:298) · History `structuredClone`이 commit마다 base64 통째 깊은복사(history.ts:38, 200스텝 누적 수GB). 처방=id→item Map/참조캐시 · lockedIdSet board변경시만 캐시 · history는 src 참조공유+메타만 복제. *(노력: S~M, history 정공법 L)*
- [x] 🟠 **이미지 import 직렬** (main.ts:2185) — 디코드/배치 둘다 for+await 순차. 동시성 캡(4~8) 병렬화. *(노력: S~M)*
- [x] 🟠 **pack 메인스레드 O(25·n²)** (main.ts:947, pack.ts findBestLayout 25회 동기) — 수백장 정돈 시 UI 프리즈. pack.ts 순수함수라 Worker화 용이. *(노력: M)*
- [x] 🟠 **srcset 해상도마다 원본 재디코드** (srcset.ts:60, 3종 각각 downscaleIfLarge=3회 디코드) — 1회 디코드 재사용. (공유 export 시점만) *(노력: M)*
- [x] 🟠 **접근성: 에디터 모달 5종 포커스 트랩 없음** (command-palette·settings·share-dialog·board-manager·recent-picker, Tab 캔버스로 샘·aria-modal만 선언) — 뷰어 lightbox.ts:360에 구현 있으니 공통헬퍼 추출. *(노력: M)*
- [x] 🟠 **에디터 터치 제스처 전무** (touch.ts `attachTouchGestures`가 뷰어만, 에디터는 팬=우/휠클릭·줌=휠뿐 → 태블릿 핀치/팬 불가) — 1손가락=선택 충돌이라 2손가락만 터치 팬/핀치 분기. *(노력: M~L)*
- [x] 🟠 **죽은 코드 정리** — `share-export.ts`(133줄 전체 미사용) · `viewer/lazyload.ts`(createLazyLoader 모듈 전체) · `setupInstallPrompt`(viewer/pwa.ts:51 미배선) · 미사용 export ~27건(virtualize dev유틸·recent setMaxRecent류·keymap getAction/parseCombo 등) · refb `opts.mode` 사문화. 기능계획 없으면 삭제, 있으면 배선. *(노력: S)*
- [x] 🟠 **공유 UX** — 업로드가 사라지는 토스트뿐(진행률·버튼 disable 없음→중복클릭) · `signUrls` 부분실패 시 키 잔존→검정인데 console.warn만(supabase-share.ts:321, crop/GIF src는 missing 추적도 안됨). showLoading 영구표시 + 버튼 disable. *(노력: M)*
- [x] 🟠 **DRY / 매직상수 → `constants.ts`** — `fileToDataURL`(main.ts:2262)≡`blobToDataURL`(이미 import중→로컬삭제만) · `downloadBlob`≡`downloadHtml` · base64디코드 2곳 · z-index `10000` 5개 모달 · `4096` 2곳 · 줌범위 `0.05/20` 3곳(에디터/뷰어 불일치 위험). *(노력: 각 S)*

### 7.3 🟡 빠른 손질 · 폴리시
- [x] 🟡 **상태바 "커서" 좌표** — host pointermove서 screenToWorld→updateStatus({cursor}). ✅ (커밋 02c8b2c) ※ 핫패스 throttle은 7.4-1로 분리
- [x] 🟡 **prompt()/confirm() → 모달** — 보드 이름변경·댓글편집·확인창을 `dialog.ts`/`dialog-shell.ts` 모달로(modal.ts 포커스트랩 재사용). ✅ (커밋 618ed01)
- [x] 🟡 **빈 캔버스 hint에 가져오기/열기 버튼** — index.html 빈 상태 안내에 `이미지 가져오기`/`보드 열기` 버튼(마우스-only 시작 경로 보강). ✅ (커밋 e6595bb)
- [~] 🟡 **main.ts God-file 2266줄 점진 분리** — note-editor/cursor reporter 분리 완료. 후보 잔여: pointer-input(onPointerDown/Move/Up+DragState 655~901, 247줄) · drawing-tool · action-dispatcher(runAction 103줄+keydown) · share-io. 한 모듈씩. *(노력: L)*
- [x] 🟡 **자잘한 UX 폴리시** — 빈 캔버스 버튼 ✅(위 항목). 라이트박스 터치 핀치/스와이프, 뷰어 만료 액션버튼, 다중탭 autosave 충돌 방지까지 반영.

### 7.4 🟠 검수 후속 보완 (2026-06-27 Claude 검수 발견, 미처리)
> 7.1~7.3 검수는 합격(빌드 0·테스트 19/19). 아래는 그 과정에서 추가로 발견된 후속(치명 아님). 상세: `docs/HANDOFF-codex.md` §10.
- [x] 🟠 **커서 좌표 pointermove 핫패스** — rAF 코얼레싱 reporter로 상태바 갱신을 묶고, 팬 경로의 `getBoundingClientRect` 호출은 그대로 두되 상태바 재기록을 줄였다. *(노력: S~M)*
- [x] 🟢 **`share-dialog.ts` 모달 중복(기존 부채)** — `dialog-shell.ts`로 통합해 백드롭/포커스트랩/버튼 셸 중복을 제거했다. *(노력: M)*

### 7.5 🔴 전체 버그 감사 후속 (2026-06-28 Claude 4차원 감사 발견, 미처리) → Codex 인계
> 데이터무결성·`main.ts`·viewer·UI 4차원 병렬 감사. 검수자 직접 대조 확정분. 상세 처방·패치: `docs/HANDOFF-codex.md` §12.
- [x] 🔴 **P1 노트 유실** — `restore()` 후 `note-editor.ts`가 stale `board` 참조(부팅 시점 객체 고착). 새 노트가 live `board.items`에 안 들어가 저장/undo서 유실. §11.1이 절반만 고친 잔존 회귀(`getNode` 캐스팅만 고치고 참조 고착은 남음). 처방=createNoteEditor 호출의 `board,`→`get board(){return board}` + restore 시뮬 회귀테스트. (HANDOFF §12.1) ✅
- [x] 🟠 **P2 IME Enter ×2** — `dialog.ts:57`(prompt 단일행)·`command-palette.ts:307`(팔레트)가 `isComposing` 미가드 → 한글 조합 확정 Enter가 즉시 제출/명령실행. (HANDOFF §12.2·12.3) ✅
- [x] 🟡 **P2 경미(여유 시)** — viewer 핀치 줌 중심 미보정·lightbox 단일이미지 화살표 순환·잠긴 단일 move 빈진입·autosave 다탭 TOCTOU. (HANDOFF §12.4) *핀치 줌은 오탐이라 미수정, autosave는 현행 유지*
- [ ] 🟢 **P3 굳히기(선택)** — supabase `orig` 가드·arrange-sort addedAt/z 혼합·toolbar innerHTML 폴백·export scale 상한. (HANDOFF §12.5)

### 진행 현황 (Phase 7, 2026-06-28 기준)
- ✅ **완료**: 7.1 견고성 4건 · 7.2 성능/접근성/정리 9건 · 7.3 폴리시 3건(커서·모달·빈캔버스버튼) · 7.3 자잘한 UX 3건 · 7.4 검수후속 2건 · **1차 검수후속 4건**(클라우드 load 검증·노트/드로잉 가시화·concurrency 정리 등 — 상세 `docs/HANDOFF-codex.md` §10, 모두 검수 합격)
- ⬜ **잔여**: 7.3 main.ts God-file 분리(L) · **7.5 잔여**(🟢P3 굳히기 — `docs/HANDOFF-codex.md` §12.5)
- ✅ **검수후속 반영**: `docs/HANDOFF-codex.md` §11의 노트 재편집/삭제, Enter 확인, 커서 reflow 보완 완료
- 검증: `npm run test` 25/25 · `npm run build` 0 에러 (2026-06-28)

---

## 부록 — 현재 코드 맵
| 파일 | 역할 | 상태 |
|---|---|---|
| `app/src/core/board.ts` | 보드 상태 모델(SSOT) · 직렬화 (+name/addedAt) | ✅ |
| `app/src/core/scene.ts` | PixiJS 렌더러 · 카메라 · 포인터 · 선택오버레이 · AABB | ✅ |
| `app/src/main.ts` | 입력 배선(선택/이동/줌/팬/드롭/단축키/runAction) | ✅ |
| `app/src/core/selection.ts` | 선택 상태 집합 + 변경 구독 | ✅ (1.3) |
| `app/src/core/pack.ts` | 자동 패킹(Skyline bin packing) | ✅ (1.5) |
| `app/src/core/arrange-sort.ts` | 기준별 격자정렬 🆕 | ✅ (1.5) |
| `app/src/core/io.ts` / `refb.ts` | .refb 저장/열기 · ZIP 포맷 | ✅ (1.7/3.1) |
| `app/src/core/history.ts` | Undo/Redo(상태 스냅샷) | ✅ (1.8) |
| `app/src/core/zorder.ts` | z순서(맨앞뒤/한단계/normalizeZ) | ✅ (1.6) |
| `app/src/core/minimap.ts` / `snap.ts` | 미니맵 · 그리드/이웃 스냅 | ✅ (1.1/1.4) |
| `app/src/core/gizmo.ts` / `crop.ts` | 변형 기즈모 · 비파괴 크롭 | ✅ (2.1/2.2) |
| `app/src/core/align.ts` | 정렬/분배/정규화(+면적) | ✅ (2.4) |
| `app/src/core/group.ts` / `grid.ts` | 그룹 · 배경 그리드 | ✅ (2.6/2.8) |
| `app/src/core/style-control.ts` / `opacity-control.ts` | 노트/드로잉 스타일 · 투명도 | ✅ (2.7/2.9) |
| `app/src/core/eyedropper.ts` | 색상 스포이드 🆕 | ✅ (2.10) |
| `app/src/core/export-image.ts` | 내보내기(PNG/JPG/BMP) | ✅ (3.3) |
| `app/src/core/recent.ts` / `recent-picker.ts` | 최근 파일 데이터 · 모달 🆕 | ✅ (3.4) |
| `app/src/core/autosave.ts` | 자동저장/크래시 복구(IndexedDB) | ✅ (3.2) |
| `app/src/core/keymap.ts` / `command-palette.ts` | 단축키·재바인딩 · 커맨드 팔레트 | ✅ (4.5) |
| `app/src/core/theme.ts` / `settings-panel.ts` / `toolbar.ts` | 테마 · 설정 · 툴바/상태바 | ✅ (4.4) |
| `app/src/core/downscale.ts` / `virtualize.ts` | 다운스케일 · 가상화 | ✅ (4.6) |
| `src-tauri/` | Tauri 데스크탑 셸 · 윈도우 모드 | ✅ (4.x) |
| `app/src/viewer/` | 읽기 전용 웹 뷰어(lightbox/touch/pwa/lazyload) | ✅ (5.2/5.3) |
| `app/src/core/share-*.ts` / `supabase-share.ts` / `srcset.ts` | 공유 어댑터 · Supabase · 다중해상도 | ✅ (5.1) |
